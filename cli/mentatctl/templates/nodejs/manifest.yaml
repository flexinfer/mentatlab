id: {{AGENT_ID}}
version: {{VERSION}}
image: {{AGENT_ID}}:{{VERSION}}
runtime: nodejs18
description: "{{DESCRIPTION}}"
inputs:
  - name: text
    type: string
outputs:
  - name: result
    type: string
longRunning: false
resources:
  cpu: "100m"
  memory: "128Mi"
env:
  - "NODE_ENV=production"